# AI Project Guardian

このリポジトリは、AIエージェントの設計診断・権限制限・実行検証を行うAgent Guardianです。

## 実装方針

- 主画面は`/`、APIは`POST /api/launch`です。
- 日本語で仕様、根拠、リスク、通常状態を説明します。英語はSF演出や短い状態コードに限ります。
- Geminiの出力は構造検証し、引用できない主張を事実として扱いません。
- AIは制限を提案し、プログラムの認可層が制限を強制します。
- 不明な設定は安全とみなさず、PENDまたは未確認として表示します。
- 実行検証は合成データの問い合わせ返信環境に限ります。
- APIキー、認証情報、個人情報、実案件データをコミットしません。
- 変更後はTypeScript、ESLint、単体テスト、本番ビルドを確認します。

## 中核ファイル

- `app/page.tsx`: 起動審査画面
- `app/api/launch/route.ts`: 診断・制限付き実行API
- `lib/launch/workflow.ts`: 診断、修正、再検証フロー
- `lib/launch/sandbox.ts`: 合成データと実行時認可
- `lib/launch/permit.ts`: 期限付き署名許可
- `lib/launch/report.ts`: 共有用診断レポート

最優先する考え方は、AIの答えだけでなく、その判断を信頼できる根拠と、危険操作を実際に止めた結果を示すことです。
